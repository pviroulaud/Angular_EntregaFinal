import { ApellidonombrePipe } from './apellidonombre.pipe';

describe('ApellidonombrePipe', () => {
  it('create an instance', () => {
    const pipe = new ApellidonombrePipe();
    expect(pipe).toBeTruthy();
  });
});
